// Alias the generator for `Datum` since `Datum` is a type alias for
// `PlutusData`
import * as TestPlutusData from "../PlutusDataInstances-test.js";

export const fcRedeemer = TestPlutusData.fcPlutusData;
